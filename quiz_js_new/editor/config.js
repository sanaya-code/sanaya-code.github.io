// editor/config.js
// Only constants that are not type-specific.
// All per-type data (label, color, formTag, previewTag, default)
// lives in editor_form_registry.js

const EditorConfig = {

  DIFFICULTY_LEVELS: ['easy', 'medium', 'hard'],

  SKIP_TYPE: 'skip',

  STORAGE_KEY: 'quiz_editor_draft',

};