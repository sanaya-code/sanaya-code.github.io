// Controller
const subjectData = { Math, Science, English };
const tabComponent = document.createElement('tab-component');
tabComponent.data = { subjects, subjectData };
document.body.appendChild(tabComponent);
